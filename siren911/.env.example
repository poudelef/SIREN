# Copy this file to .env and fill in your keys (or just paste them into the
# sidebar at runtime — the app reads ANTHROPIC_API_KEY from the environment
# as a convenience default, but the sidebar field always works too).

ANTHROPIC_API_KEY=sk-ant-...
DEEPGRAM_API_KEY=...
